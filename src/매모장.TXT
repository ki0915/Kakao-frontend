//컴포넌트는 불값 빼면 다 출력가능
//props 가 부모 컴포넌트에서 보내는 값이다.
//이 코드 컴포넌트는 buttontype에 맞춰 가공 되어진다.
//여기 태그 안, name이나 title 부분에 컴포넌트 값을 넣으니 그 값으로 화면이 출력되었다.
// return 안에 코드가 들어가는 부분은 {}로 감싸져있어야함. 그리고 그 부분은 또 태그로 감싸져 있어야 함. 주로 빈 태그 <> </>로 감쌈
// react는 조건 1 &&  컴포넌트 인경우 컴포넌트가 실핼됨
// 삼항 연산자는  ? 그 참일 때: 아닐 때 실행할 컴포넌트 선택 가능
// 가장큰 레벨의 태그는 하나만 있어야 함
// 이벤트 객체 내에서 preventDefault라는 함수는 이벤트 취소하는 기능
// stopPropagtion은 이벤트의 전파를 막음